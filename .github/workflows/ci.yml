name: ci

on:
  workflow_dispatch:
  push:
    branches: [main, "phase*", feat/aws-probability-platform-20260717]
  pull_request:

permissions:
  contents: read

jobs:
  release-gate:
    uses: ./.github/workflows/release-gate.yml
