# Minimal dependencies for the autonomous NRFI pipeline (CI + Actions).
# The legacy prototype's heavier stack lives in requirements.txt.
pandas>=2.1
numpy>=1.26
scikit-learn>=1.4
requests>=2.31
joblib>=1.3
tabulate>=0.9
pytest>=7.4
ruff>=0.4
